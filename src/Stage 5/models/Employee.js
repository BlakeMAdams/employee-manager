import React, { Component } from 'react';

export default class Employee {
  // constructor
  constructor(id, name, phone, title) {
    this.id = id;
    this.name = name;
    this.phone = phone;
    this.title = title;
  }


  // updateName
  updateName(str) {
    this.prop.name = str;
  }

  // updatePhone
  updatePhone(str) {
    this.prop.name = str;
  }
  // updateTitle
  updateTitle(str) {
    this.prop.name = str;
  }
}